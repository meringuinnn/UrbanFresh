"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var _a;
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const logger_1 = require("@expo/logger");
const uuid_1 = require("uuid");
const BuildConfigParser_js_1 = require("../BuildConfigParser.cjs");
const BuildStepContext_js_1 = require("../BuildStepContext.cjs");
const errors_js_1 = require("../errors.cjs");
const logger = (0, logger_1.createLogger)({
    name: 'steps-cli',
    level: 'info',
});
async function runAsync(configPath, workingDirectory, runtimePlatform) {
    const fakeBuildId = (0, uuid_1.v4)();
    const ctx = new BuildStepContext_js_1.BuildStepContext(fakeBuildId, logger, false, runtimePlatform, workingDirectory);
    const parser = new BuildConfigParser_js_1.BuildConfigParser(ctx, { configPath });
    const workflow = await parser.parseAsync();
    await workflow.executeAsync();
}
const relativeConfigPath = process.argv[2];
const relativeWorkingDirectoryPath = process.argv[3];
const platform = ((_a = process.argv[4]) !== null && _a !== void 0 ? _a : process.platform);
if (!relativeConfigPath || !relativeWorkingDirectoryPath) {
    console.error('Usage: yarn cli config.yml path/to/working/directory [darwin|linux]');
    process.exit(1);
}
const configPath = path_1.default.resolve(process.cwd(), relativeConfigPath);
const workingDirectory = path_1.default.resolve(process.cwd(), relativeWorkingDirectoryPath);
runAsync(configPath, workingDirectory, platform).catch((err) => {
    logger.error({ err }, 'Build failed');
    if (err instanceof errors_js_1.BuildWorkflowError) {
        logger.error('Failed to parse the custom build config file.');
        for (const detailedErr of err.errors) {
            logger.error({ err: detailedErr });
        }
    }
});
//# sourceMappingURL=cli.js.map